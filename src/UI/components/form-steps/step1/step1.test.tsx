import { render } from "@testing-library/react";

describe("step1 page component", () => {
  xit("should render the component correctly", async () => {
    // Implementação do teste será adicionada posteriormente
  });
});
